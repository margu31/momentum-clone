import styled from 'styled-components';

const StyledSignContainer = styled.div`
  display: inline-flex;
  position: absolute;
  top: 1rem;
  right: 5rem;
  padding: 1rem;
`;

export default StyledSignContainer;
